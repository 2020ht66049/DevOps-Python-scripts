# Get user input for two numbers
number1 = float(input("Enter first number: "))
number2 = float(input("Enter second number: "))

# Add the two numbers and store the result in 'sum'
sum = number1 + number2

# Print the sum
print("The sum is:", sum)

Explaination:
Conclusion:

This Python program allows users to input two numbers, adds them together, and displays the sum. The use of the input() function to capture user input, the int() function to convert the input to integers, and the print() function to output the result makes the program simple yet effective for basic arithmetic operations.

string reverse
=============

name = "umesh"
newname = name[::-1]
print(newname)
